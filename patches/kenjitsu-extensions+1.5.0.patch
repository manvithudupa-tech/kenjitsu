diff --git a/node_modules/kenjitsu-extensions/dist/main.d.ts b/node_modules/kenjitsu-extensions/dist/main.d.ts
index 1111111..2222222 100644
--- a/node_modules/kenjitsu-extensions/dist/main.d.ts
+++ b/node_modules/kenjitsu-extensions/dist/main.d.ts
@@ -1282,7 +1282,7 @@
  *
  */
 declare class Anilist extends BaseAnimeMeta {
-    constructor();
+    constructor(provider?: string, providerUrls?: { animepahe?: string });
     private readonly baseUrl;
     /**
      * Maps an Anilist anime ID to the corresponding Aniwatch provider ID.
diff --git a/node_modules/kenjitsu-extensions/dist/main.js b/node_modules/kenjitsu-extensions/dist/main.js
index 0303d4b..a1b2c3d 100644
--- a/node_modules/kenjitsu-extensions/dist/main.js
+++ b/node_modules/kenjitsu-extensions/dist/main.js
@@ -4460,7 +4460,7 @@ var Anilist = class extends BaseAnimeMeta {
   animepahe;
   kaido;
   anizip;
-  constructor(provider) {
+  constructor(provider, providerUrls = {}) {
     let delay = null;
     if (provider === "jikan") {
       delay = 1e3;
@@ -4469,7 +4469,7 @@ var Anilist = class extends BaseAnimeMeta {
     }
     this.client = new FetchClient({ delayBetweenRequests: delay });
     this.aniwatch = new Aniwatch();
-    this.animepahe = new Animepahe();
+    this.animepahe = new Animepahe(providerUrls.animepahe);
     this.anizone = new Anizone();
     this.kaido = new Kaido();
     this.anizip = new AniZip();
@@ -5638,6 +5638,14 @@ var Anilist = class extends BaseAnimeMeta {
       }
       const anilistData = initialResponse.value.data;
       const animepaheId = initialResponse.value.provider?.id;
+      if (!animepaheId) {
+        return {
+          error: initialResponse.value.error || "Could not map Anilist ID to Animepahe provider",
+          data: anilistData || null,
+          providerEpisodes: [],
+          provider: null
+        };
+      }
       const anizipEpisodes = anizip.value.episodes;
       const animepahe = await this.animepahe.fetchEpisodes(animepaheId);
       const animepaheNumbers = animepahe.data.map((e) => Number(e.episodeNumber));
